# تقرير المراجعة الشاملة لسطور الكود (Line-by-Line Audit Report) - مستودع Tsstawy

> **التاريخ:** 11 سبتمبر 2026  
> **المراجع:** فريق الخبراء الـ 20 الاستشاري  
> **حالة المستودع:** Public | GitHub Pages: `https://gimy404.github.io/Tsstawy/`  
> **نقطة الأمان المحفوظة (Safety Tag):** `backup/pre-refactor-2026-09-11` (تم التحقق منها على السيرفر)

---

## 1. الهيكل العام والمجلدات (Structure)

### الوضع الحالي لشجرة المجلدات:
المستودع ناتج عن عملية سحب/نسخ لموقع ووردبريس قديم (`muradmurad.com`) عبر أداة سحب صفحات (Scraper):
- وجود 8 ملفات HTML رئيسية في المجلد الجذري: `index.html`, `SEM.html`, `branding.html`, `content-writing.html`, `influencers.html`, `mystery-shopper.html`, `social-media-management.html`, `website-design.html`.
- وجود مجلد كامل باسم `muradmurad.com` بداخله نسخة مكررة من `index.html` ومجلدات `wp-content` و `wp-includes`.
- وجود مجلدات لخدمات تتبع وإعلانات مسحوبة بالخطأ:
  - `googleads.g.doubleclick.net`
  - `pagead2.googlesyndication.com`
  - `tpc.googlesyndication.com`
  - `www.google.com` (recaptcha iframes)
  - `www.googletagmanager.com`
  - `_DataURI`

### المشاكل المكتشفة:
1. **تشتت شديد وتكرار للملفات:** وجود نسختين من الصفحة الرئيسية (`index.html` و `muradmurad.com/index.html`).
2. **ملفات مهملة وشفرات تتبع خارجية:** وجود إعلانات وملفات iframe خاصة بجوجل دبل كليك لا علاقة لها بمعرض الأعمال وتثقل المستودع.
3. **غياب التنظيم المعياري:** غياب مجلدات واضحة للأصول مثل `assets/css`, `assets/js`, `assets/images`.

---

## 2. جودة الكود ونظافته (Code Quality)

1. **ملفات ضخمة غير منسقة (Giant Files):**
   - ملف `index.html` يحتوي على **4,500 سطر** من كود Elementor المسحوب والمضغوط في بعض الأجزاء.
2. **استدعاء ملفات مفقودة تسبب خطأ 404:**
   - في `index.html:3`: يتم استدعاء `<link rel="stylesheet" href="styles.css">` بينما ملف `styles.css` غير موجود نهائياً بالمستودع.
3. **شفرات ميتة وبقايا ووردبريس (Dead Code):**
   - سكريبت كشف الإيموجي لووردبريس (`wpemojiSettings` في `index.html:45-144` وباقي الصفحات).
   - دوال فحص الكانفاس والـ WebWorker التي لا فائدة منها في موقع ثابت.
4. **تكرار الكود (Duplication):**
   - كل صفحة فرعية تكرر نفس الهيدر والفوتر والسكربتات بالكامل بدلاً من توحيدها.

---

## 3. الأمان وتسريب البيانات (Security) — 🔴 حرج

1. **نموذج التواصل يرسل البيانات لسيرفر خارجي (Data Leakage & Broken Forms):**
   - في `index.html:3252` و `index.html:3636`: نموذج الاتصال (`<form>`) مبني بإضافة Caldera Forms القديمة ويرسل الطلبات إلى:
     `data-request="https://muradmurad.com/cf-api/CF5c59ecfb6e521"`
   - **الخطورة:** إذا كتب أي عميل اسمه، إيميله، ورقم هاتفه ورسالته، فإن النموذج يحاول إرسالها لموقع ووردبريس لشخص آخر (`muradmurad.com`) مع مفتاح حماية ووردبريس منتهي الصلاحية منذ فبراير 2024 (`nonce-time="1708438663"`). النموذج يفشل حالياً تماماً في الإرسال (CORS Error) وفي حال عمله سيسرب بيانات العملاء.
2. **شفرات تتبع وتحقق تخص شخصاً آخر:**
   - رمز إثبات ملكية جوجل: `<meta name="google-site-verification" content="38nNXGj-Jl59BmA-m6kfcLVrAEPXTUu-Q9kQ3wQIzx8" />` (يخص موقع مراد مراد).
   - معرفات Google Tag Manager و Google Adsense القديمة.

---

## 4. التبعيات والمكتبات (Dependencies)

1. **تحميل مكتبة FontAwesome بشكل مكرر 8 مرات:**
   - في `index.html:261-310`: يتم تحميل FontAwesome 5.15.4 و FontAwesome 6.5.1 و v4-shims و solid و brands في نفس الصفحة، مما يسبب بطء غير مبرر وتعارض في الأيقونات.
2. **ربط ملفات التنسيق والسكربتات بسيرفر خارجي (Hotlinking):**
   - جميع صفحات الموقع تستدعي ملفات الـ CSS والـ JS عبر روابط مباشرة لـ `https://muradmurad.com/wp-content/cache/...`. لو توقف هذا الموقع أو حجب الروابط الخارجية، سيفقد موقعك تصميمه بالكامل فوراً.

---

## 5. الأخطاء البرمجية المعطلة (Bugs)

1. **تضارب الهوية (Identity Mismatch):**
   - الصفحة الرئيسية تم تعديل نصوص منها لتصبح باسم **Mohamed Gamal** ووضع أرقام واتساب مصر (`+201098726800`) وإيميل `m.gamal.zaid1@gmail.com`.
   - لكن في نفس الصفحة لا تزال هناك روابط للواتساب القديم (`+971565533343` في السطر 3567) ورابط إنستجرام وفيسبوك لمراد مراد.
   - الصفحات الفرعية السبعة (`branding.html`, `SEM.html`, إلخ) لا تزال بالكامل باسم مراد مراد وبياناته السابقة ولم يتم تعريبها أو تعديلها لاسم محمد جمال.

---

## 6. واجهة وتجربة المستخدم (UI/UX)

1. **غياب دعم التصفح بالعربية (RTL):** الموقع موجه لسوق الإمارات والخليج ومصر، ولكنه مكتوب بالكامل باللغة الإنجليزية بدون نسخة عربية أو دعم اتجاه الكتابة من اليمين لليسار (RTL).
2. **روابط شبكات التواصل:** بعض الروابط تفتح حسابات قديمة لشخص آخر، مما يشتت العميل ويفقد الثقة.
3. **أزرار التواصل والنماذج:** زر إرسال الرسالة معطل بسبب مشكلة الفورم المذكورة في الأمان.

---

## 7. تحسين محركات البحث (SEO) — 🔴 حرج جداً

1. **كارثة الكانونيكال (Canonical Tag Trap):**
   - في `index.html:25` وكل الصفحات:
     `<link rel="canonical" href="https://muradmurad.com/" />`
   - **المعنى التقني:** أنت تخبر محركات البحث (جوجل) صراحةً أن موقعك هذا هو مجرد "نسخة منقولة غير أصلية" وأن الموقع الأصلي هو `muradmurad.com`. هذا يجعل جوجل يحذف موقعك من نتائج البحث ولا يمنحه أي ترتيب!
2. **بيانات الميتا والمشاركة (Open Graph & Twitter Cards):**
   - العنوان، الوصف، والشعار المعروض عند مشاركة الرابط على واتساب أو لينكدإن يشير إلى "Murad Murad".
3. **غياب ملفات الفهرسة:** لا يوجد ملف `sitemap.xml` أو `robots.txt` لموقعك على GitHub Pages.

---

## 8. الأداء والسرعة (Performance)

1. استدعاء خطوط وملفات CSS من سيرفرات خارجية غير مستقرة.
2. وجود مجلدات وصور قديمة مكررة في `wp-content/uploads`.
3. غياب التخزين المؤقت المحلي (Local Assets Caching).

---

## 9. التوثيق (Documentation)

1. المستودع لا يحتوي على ملف `README.md` يوضح ما هو المشروع، لمن يتبع، وكيف يتم تشغيله وتعديله.
2. لا يوجد دليل للملفات أو كيفية نشر التحديثات على GitHub Pages.

---

## 10. جدول ملخص الملاحظات ومستوى خطورتها (Severity Table)

| المشكلة | التصنيف | مستوى الخطورة | التأثير |
|---|---|---|---|
| رابط الكانونيكال (Canonical) يشير للموقع الأصلي | SEO | 🔴 حرج | جوجل يعتبر موقعك منسوخاً ولا يقوم بأرشفته باسمك |
| نموذج الاتصال يرسل بيانات العملاء لسيرفر خارجي معطل | Security / Bugs | 🔴 حرج | فشل استقبال أي رسائل من العملاء مع مخاطرة أمنية |
| ملف `styles.css` مفقود ويعطي خطأ 404 | Code Quality | 🔴 حرج | خطأ صريح في طلبات الشبكة بالصفحة الرئيسية |
| ملفات الـ CSS/JS مربوطة بسيرفر خارجي (Hotlinking) | Performance / Infra | 🟠 مهم | في حال توقف موقع المصدر ينهار مظهر الموقع كاملاً |
| تضارب الهوية والروابط (محمد جمال vs مراد مراد) | Branding / UX | 🟠 مهم | إرباك العميل وفقدان المصداقية الاحترافية |
| تحميل مكتبات مكررة 8 مرات (FontAwesome) | Performance | 🟠 مهم | بطء تحميل الصفحة واستهلاك باقة الزائر |
| مجلدات مسحوبة مشوشة للمستودع (Google Ads / DoubleClick) | Structure | 🟡 تحسين | حجم زائد وتشويش في شجرة الملفات |
| غياب ملفات الفهرسة والتوثيق (README / Sitemap / Robots) | Documentation / SEO | 🟡 تحسين | صعوبة إدارة المشروع من المالك في المستقبل |
